export interface songInterfaces {
  idSong: number;
  name: string;
  url: string;
  time: number;
  creation: string;
  update: string;
  type: string;
  attributInsert ? : Array < string > ;
}